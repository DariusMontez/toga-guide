{{template}} example from the guide
===============

More information about this example can be found in the `Toga Guide`_.


Quickstart
~~~~~~~~~~

To run this example:

    > pip install guide-{{template}}
    > python -m switch

